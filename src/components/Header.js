import React from 'react';
//import {inject, observer} from'mobx-react';

import PWAppBar from "./PWAppBar";

const Header = () => {
  return (
    <header className="App-header">
      <PWAppBar/>
    </header>
  );
};

export default Header;
